import React from 'react';
import { Navigate } from 'react-router-dom';

const BlogPost = () => {
    // Placeholder - redirect to blog index
    return <Navigate to="/blog" />;
};

export default BlogPost;
